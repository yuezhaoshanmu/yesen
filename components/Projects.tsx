import SystemDataFlow from './effects/SystemDataFlow';
import Image from 'next/image';
import { ArrowUpRight, ExternalLink } from 'lucide-react';
import { projects } from '@/data/projects';
import SectionHeading from './SectionHeading';
import Spotlight from './Spotlight';

export default function Projects(){return <section id="projects" className="section container section-anchor"><div className="projects-heading"><SectionHeading index="09" eyebrow="ENGINEERING / FROM IDEA TO PRODUCT" title={<>全栈项目实践，<br/><span className="subtle">让想法成为真实产品。</span></>} description="代码的价值，也在浏览器被打开的那一刻。三个真实项目，三个不同的应用场景。"/><span className="micro project-count">SELECTED WORKS / 03</span></div><SystemDataFlow/><div className="projects-grid">{projects.map((project,i)=><Spotlight key={project.id} className={`project-card ${project.color}`} tilt><a className="project-visual" href={project.url} target="_blank" rel="noopener noreferrer" aria-label={project.domain+'，打开'+project.name+'在线项目'}><div className="browser-chrome"><span className="browser-dots"><i/><i/><i/></span><span>{project.domain}</span><ExternalLink size={11}/></div><div className="project-screenshot"><Image src={project.image} alt={project.name+'真实在线页面截图'} width={1440} height={1000} sizes="(max-width: 700px) 90vw, 33vw"/></div><span className="project-visit"><ArrowUpRight size={22}/></span></a><div className="project-details"><div className="project-eyebrow"><span className="micro">0{i+1} / LIVE PROJECT</span><span><i className="status-dot"/> 已连接真实站点</span></div><h3>{project.name}</h3><p>{project.description}</p><div className="project-tags">{project.tags.map(t=><span key={t}>{t}</span>)}</div><a href={project.url} target="_blank" rel="noopener noreferrer" className="project-link">访问项目<span>Explore Project <ArrowUpRight size={16}/></span></a></div></Spotlight>)}</div><p className="disclosure">预览来自项目真实页面。站点功能说明以其页面为依据。</p></section>;}
